# Some packages (react-material-ui-carousel, vite-plugin-pwa) have not yet declared
# support for React 19 / Vite 8 in their peer dependency ranges, even though they work
# correctly at runtime. This flag allows npm install to resolve without errors.
legacy-peer-deps=true
